#include <iostream>
#include <cmath> 
using namespace std;
int main()
{
	setlocale(0, "");
	double x = 3.25, y = 4.12, b, z;
	z = 2 * sin(3 * x + y);
	b = (x + y * (x * x + cos(y))) / (y * (x - z) + log(x * z));
	cout << " функция b = " << b << endl;
	return 0;
}
